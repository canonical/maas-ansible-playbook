---
- name: "Uninstall Pacemaker Packages"
  ansible.builtin.apt:
    name: "{{ item }}"
    state: absent
    purge: true
  with_items:
    - pcs
    - pacemaker
    - fence-agents
    - resource-agents-paf
